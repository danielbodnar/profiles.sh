---
interface Props {
  name: string;
  subtitle: string;
  meta: string;
  avatarUrl?: string;
}

const { name, subtitle, meta, avatarUrl } = Astro.props;
---
<header class="profile-header">
  <div class="label">profiles.sh</div>
  {avatarUrl && (
    <img src={avatarUrl} alt={name} class="avatar" width="64" height="64" />
  )}
  <h1 class="name">{name}</h1>
  <p class="subtitle">{subtitle}</p>
  <p class="meta">{meta}</p>
</header>

<style>
  .profile-header {
    text-align: center;
    margin-bottom: 12px;
  }

  .label {
    font-size: 0.8125rem;
    font-family: "SF Mono", "Cascadia Code", "Fira Code", Consolas, monospace;
    color: #444;
    letter-spacing: 4px;
    text-transform: uppercase;
    margin-bottom: 8px;
  }

  .avatar {
    width: clamp(3rem, 5vw, 4rem);
    height: clamp(3rem, 5vw, 4rem);
    border-radius: 50%;
    border: 1px solid rgba(255, 255, 255, 0.1);
    margin-bottom: 10px;
  }

  .name {
    margin: 8px 0 0;
    font-size: 2.25rem;
    font-weight: 200;
    color: #fff;
    letter-spacing: -0.03em;
  }

  .subtitle {
    margin: 6px 0 0;
    font-size: 0.875rem;
    color: #555;
    font-family: "SF Mono", "Cascadia Code", "Fira Code", Consolas, monospace;
  }

  .meta {
    margin: 4px 0 0;
    font-size: 0.8125rem;
    color: #444;
    font-family: "SF Mono", "Cascadia Code", "Fira Code", Consolas, monospace;
  }
</style>
